[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pedgen"
version = "0.0"

[tool.setuptools]
packages = ["pedgen"]

[tool.yapf]
based_on_style = "google"
indent_width = 4
column_limit = 80